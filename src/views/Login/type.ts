export type LoginFormType = {
    email: string,
    password: string,
    imgValidateCode: string
}

export type RegisterFormType = {
    email: string,
    username: string,
    password: string,
    validateCode: string,
    userType: string,
}